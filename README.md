# fun

Recursively searches up for .fun directory and executes the given script. Support only for unix like OSes.
It is supper eazy to use just add your scripts to .fun directory and from any subdirectory of .fun/.. run `fun script_name` to exeute the script in the directory .fun/..